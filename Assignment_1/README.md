# 📝 Assignment 1: Markdown Syntax

## 📌 Overview
This assignment focuses on mastering **Markdown** and **HTML** formatting within Jupyter Notebooks. It covers headings, image manipulation, and layout techniques.

## 📋 Tasks Completed
1.  **Normal Headings**: Created Level 1, 2, and 3 headings.
2.  **Centered Heading**: Used HTML `<h1>` tags with `align="center"`.
3.  **Insert Image**: Used standard Markdown syntax `![Alt](url)`.
4.  **Resize Image**: Used HTML `<img>` tag with `width` attribute.
5.  **Image + Text Side by Side**: Used HTML `<table>` for layout.
6.  **3 Images Side by Side**: Used HTML `<p>` or `<div>` with inline images.
7.  **Bonus**: Centered Title + Image using a `<div>` container.

## 🚀 How to View
Open `Markdown_Homework.ipynb` in Jupyter Notebook or JupyterLab to see the rendered results.
